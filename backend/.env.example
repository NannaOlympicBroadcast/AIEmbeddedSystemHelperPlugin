# LiteLLM / OpenAI-compatible API settings
# Set the base URL for any OpenAI-compatible endpoint (e.g. local Ollama, vLLM, LM Studio)
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_API_KEY=your_api_key_here

# Model name passed to LiteLLM (prefix with provider, e.g. openai/gpt-4o)
LITELLM_MODEL=openai/gpt-4o

# Server settings
HOST=127.0.0.1
PORT=8000
